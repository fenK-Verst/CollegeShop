# Use root/example as user/password credentials
version: '3.1'

services:

  db:
    image: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 224671
      MYSQL_USER: root
      MYSQL_PASSWORD: 224671
      MYSQL_DATABASE: college_shop

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080