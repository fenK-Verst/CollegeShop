{% extends 'base.html.twig' %}

{% block body %}
    <section class="content">
        <h3>Заказы</h3>

        <table class="table">
            <tr>
                <th>№</th>
                <th>Количество</th>
                <th>Сумма</th>
                <th>Статус</th>
            </tr>
            {% for order in orders %}
                <tr>
                    {% set stat = order.stat %}
                    <td>{{ order.id }}</td>
                    <td>{{ stat.count }}</td>
                    <td>{{ stat.sum }} Тг</td>
                    <td>{{ order.statuses[order.status] }}</td>
                </tr>
            {% endfor %}
        </table>
    </section>
{% endblock %}